# CLAUDE.md

Claude Code がこのリポジトリで作業するときに最初に読むファイル。

## このプロダクトは何か

Startup Weekend に参加した学生が、**その54時間の行動を就職活動で語れる形に変換する**ツール。

対象は、行動力も成果もあるのに面接で伝えられていない学生。
原因は実績の不足ではなく、記憶が「チームで協力して優勝しました」に丸められていること。

## 読む順番

| ファイル | 中身 |
|---|---|
| `specification.md` | 仕様の本体。コンセプト、検討経緯、技術スタック、Phase分割 |
| `docs/01-input.md` | 何を入力として取るか。音声のみ確定。5項目の情報と確実性の序列 |
| `docs/02-output.md` | 何を出力に載せるか。A面/B面の項目定義、載せない判断のルール、文章作成ガイドライン |
| `docs/03-screen.md` | 画面設計・UI/UXルール。skillsへの参照、印刷制約、AIスロップ回避 |
| `skills/migiee-jp-ui-guidelines/` | 日本向けUI制作ガイドライン（実装時に参照） |
| `skills/migiee-ui-ux-check/` | UI/UXレビュー用スキル（Phase 1完了時に使用） |

## 現在地

**Phase 0（仕様確定）の途中。** コードはまだ1行も書いていない。

確定したこと：
- 入力は音声のみ（ボイスレコーダー、スマホのボイスメモ等の既存機器）
- カメラ・センサー・生体データは却下（Humanyzeの死因を回避）
- 音声から取れる情報を5項目で整理（沈黙・笑い声・トーン・発話量・内容）
- 最悪でも沈黙の検出（谷の特定）は必ず取れる

未確定のこと：
- 何台どこに置くか
- 解析を自作するかHylableを使うか
- どこまで文字起こしするか
- 話者分離が通るか（録音環境が最悪なため、実際に録るまで不明）
- 本人の関与をどこまで求めるか

次にやること：音声解析の未確定項目を詰める。その後 Phase 1（骨格）へ。

## この案件で特に守ること

1. **盛らない。** 本人が話していないことを出力に書かせない。AIが気を利かせて補完した瞬間、面接で1問掘られて全部崩れる。
2. **弱さを消さない。** 「自信度は最低だったが動いた」という構造がこのプロダクトの価値。不安や失敗を綺麗にしない。
3. **事実と解釈を分ける。** 「発言が多かった」は事実、「リーダーシップがある」は解釈。解釈には必ず根拠を併記する。
4. **未確認は未確認と書く。** 記憶が曖昧な箇所は空欄で残し、本人に確認を促す。
5. **根性論を排除する。** 睡眠時間の短さのような「頑張り」は、就活ではマイナスに振れる。判断として語れる形に変換する（詳細は `docs/02-output.md`）。

## やらないこと（スコープ外）

- カメラ・センサー・生体データの計測（専用ハード・装着型デバイスは却下確定。理由は `docs/01-input.md` §6）
- 企業向け機能（採用管理、学生の横断比較）
- 認証、課金、複数イベント対応
- Startup Weekend 公式との連携

## 開発原則

`migiee-product-dev-principles` に従う。特に：

- specification.md が固まる前にコードを書かない
- Phase ごとに区切って確認を挟む。Phase 1 完了時はスクリーンショット確認を必ず求める
- 色数は最小限。グラデーション・グロー・ネオンは使わない
- 日本語UIは `migiee-jp-ui-guidelines` を適用（本文15〜16px、行間1.6〜1.8、純黒禁止）
- GitHub は `migichan3` アカウント、**必ず Private**
